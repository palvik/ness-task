[pytest]
addopts = -v --tb=short --alluredir=reports/allure-results
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    e2e: full end-to-end cart scenario
    unit: fast isolated unit tests (e.g. price parser)
