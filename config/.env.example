# Copy to .env and adjust. .env is gitignored.
# Anything here overrides config/config.yaml at runtime.

BASE_URL=https://www.ebay.com
HEADLESS=true
BROWSER=chromium

# Login strategy: guest | stub  (see README - CAPTCHA is out of scope per task email)
LOGIN_MODE=guest
# EBAY_USERNAME=
# EBAY_PASSWORD=
